"use client";

import type { GenerateWorkoutScheduleOutput } from '@/ai/flows/generate-workout-schedule-types';
import { Card, CardContent, CardDescription, CardHeader, CardTitle, CardFooter } from '@/components/ui/card';
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from '@/components/ui/accordion';
import { ScrollArea } from '@/components/ui/scroll-area';
import { Button } from '@/components/ui/button';
import { ClipboardList, FileDown, Loader2, Zap, Info } from 'lucide-react';
import { useState } from 'react';
import { useToast } from '@/hooks/use-toast';
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";


interface WorkoutScheduleDisplayProps {
  scheduleOutput: GenerateWorkoutScheduleOutput;
}

export default function WorkoutScheduleDisplay({ scheduleOutput }: WorkoutScheduleDisplayProps) {
  const { scheduleTitle, weeklySchedule, recoveryTips, disclaimer } = scheduleOutput;
  const [isGeneratingPdf, setIsGeneratingPdf] = useState(false);
  const { toast } = useToast();

  const handleSaveAsPdf = async () => {
    const element = document.getElementById('workoutScheduleCardPrintable');
    if (!element) {
      toast({
        title: "Error",
        description: "Could not find workout schedule content to generate PDF.",
        variant: "destructive",
      });
      return;
    }

    setIsGeneratingPdf(true);
    toast({
      title: "Generating PDF...",
      description: "Your workout schedule PDF is being created.",
    });

    try {
      const html2pdfModule = await import('html2pdf.js/dist/html2pdf.bundle.min.js');
      const html2pdf = html2pdfModule.default || html2pdfModule;
      
      const opt = {
        margin: 0.5,
        filename: 'cortex-fit-workout-schedule.pdf',
        image: { type: 'jpeg', quality: 0.98 },
        html2canvas: { scale: 2, useCORS: true, logging: false }, 
        jsPDF: { unit: 'in', format: 'letter', orientation: 'portrait' },
        pagebreak: { mode: ['avoid-all', 'css', 'legacy'] }
      };
      
      await html2pdf().from(element).set(opt).save();
      toast({
        title: "PDF Generated!",
        description: "Your workout schedule has been saved as a PDF.",
      });
    } catch (error) {
      console.error("Error generating PDF for workout schedule:", error);
      toast({
        title: "PDF Generation Failed",
        description: `An error occurred: ${error instanceof Error ? error.message : String(error)}`,
        variant: "destructive",
      });
    } finally {
      setIsGeneratingPdf(false);
    }
  };


  return (
    <Card id="workoutScheduleCardPrintable" className="mt-8 w-full max-w-3xl mx-auto shadow-xl">
      <CardHeader>
        <CardTitle className="flex items-center gap-2 text-2xl">
          <ClipboardList className="text-primary" />
          {scheduleTitle || "Your Personalized Workout Schedule"}
        </CardTitle>
        <CardDescription>
          Here is the workout schedule generated by our AI. Remember to warm up before each session and cool down afterwards.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <ScrollArea className="h-[500px] w-full p-1 border rounded-md bg-card print-expandable-scroll-area">
          <Accordion type="multiple" className="w-full px-3" defaultValue={weeklySchedule.map(day => day.dayName)}>
            {weeklySchedule.map((day, index) => (
              <AccordionItem value={day.dayName || `day-${index}`} key={day.dayName || `day-${index}`}>
                <AccordionTrigger className="text-lg font-semibold text-secondary-foreground hover:no-underline">
                  {day.dayName}: <span className="text-base font-medium text-primary ml-2">{day.focus}</span>
                </AccordionTrigger>
                <AccordionContent className="pl-2">
                  <ul className="space-y-2 mt-1">
                    {day.exercises.map((exercise, exIndex) => (
                      <li key={exIndex} className="text-sm p-2 bg-muted/50 rounded-md shadow-sm">
                        <strong className="text-foreground">{exercise.name}</strong>
                        {exercise.setsAndReps && (
                          <span className="block text-xs text-muted-foreground italic mt-0.5">
                            {exercise.setsAndReps}
                          </span>
                        )}
                      </li>
                    ))}
                  </ul>
                </AccordionContent>
              </AccordionItem>
            ))}
          </Accordion>

          {recoveryTips && (
            <div className="mt-6 p-3 border-t">
              <h3 className="flex items-center gap-2 text-lg font-semibold text-primary mb-2">
                <Zap className="w-5 h-5" />
                Recovery & General Tips
              </h3>
              <p className="text-sm whitespace-pre-line leading-relaxed text-muted-foreground">{recoveryTips}</p>
            </div>
          )}
          
        </ScrollArea>
         {disclaimer && (
            <Alert variant="default" className="mt-6 bg-amber-50 border-amber-300 text-amber-800 print-expandable-scroll-area">
              <Info className="h-5 w-5 text-amber-600" />
              <AlertTitle className="font-semibold text-amber-700">Important Disclaimer</AlertTitle>
              <AlertDescription className="text-xs">
                {disclaimer}
              </AlertDescription>
            </Alert>
          )}
      </CardContent>
      <CardFooter className="justify-end">
         <Button 
          variant="outline" 
          onClick={handleSaveAsPdf}
          disabled={isGeneratingPdf}
          className="print-hide-button w-full md:w-auto"
        >
          {isGeneratingPdf ? (
            <Loader2 className="mr-2 h-4 w-4 animate-spin" />
          ) : (
            <FileDown className="mr-2 h-4 w-4" />
          )}
          {isGeneratingPdf ? "Generating..." : "Save as PDF"}
        </Button>
      </CardFooter>
    </Card>
  );
}
